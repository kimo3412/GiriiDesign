# 页面重构方案记录

> 记录时间：2026-04-15
> 目的：把当前确认过的后台页面优化方向固化下来，方便后续在额度不足或会话中断后继续推进。

## 总体判断

当前后台页面不适合继续沿用“所有列表页都套同一套模板”的方式演进。
后续应该按业务场景拆分布局模式，并先沉淀一层可复用的视觉语义和业务组件，再逐页替换。

推荐实施顺序：

1. 先建立语义化视觉 Token 和业务原子组件
2. 再按场景改造列表页布局
3. 最后重构核心流程页

---

## 一、列表页重构原则

列表页不再一刀切地使用“统计卡片 + 筛选区 + 表格 + 分页”的固定结构，而是拆成两类模式。

### 模式 A：高频作业型

适用页面：

- 意向池
- 订单列表

核心问题：

- 用户需要高频查看详情并立刻处理
- 在“列表页”和“详情页”之间反复跳转，效率低

推荐方案：

- 使用 `Master-Detail` 主从分栏布局
- 左侧约 `1/3`：精简列表，只展示编号、状态、关键时间、优先级
- 右侧约 `2/3`：详情视窗，点击左侧后无跳转刷新详情

设计目标：

- 像处理邮件一样连续处理意向和订单
- 降低页面切换成本

落地建议：

1. 优先把 `web-admin/src/views/request/list/index.vue` 改成 `Master-Detail`
2. `web-admin/src/views/order/list/index.vue` 先保留传统表格模式，同时增加一个“作业模式”切换

### 模式 B：数据维护型

适用页面：

- 物料管理
- 客户管理
- 作品管理

核心问题：

- 用户更关注检索、筛选、批量维护
- 不需要长时间停留在详情视图

推荐方案：

- 使用 `Directory` 目录流布局
- 顶部仅保留高频组合筛选
- 不常用筛选项折叠
- 下方保持紧凑型表格
- 对客户、物料等可引入左侧分类树或维度树

设计目标：

- 快速定位数据
- 提升批量维护效率

优先适用页面：

- `web-admin/src/views/customer/list/index.vue`
- `web-admin/src/views/supply/material/index.vue`
- `web-admin/src/views/supply/inventory/index.vue`
- `web-admin/src/views/portfolio/list/index.vue`

---

## 二、主流程页重构原则

### 目标

订单详情、工作台等核心页面不再采用“长页面线性下滚”的组织方式，而是改成 `Bento Dashboard` 风格的控制台布局。

设计目标不是“首屏展示全部”，而是“首屏完成判断”。
用户在 1080P 的首屏内，应完成 80% 的状态认知与下一步决策。

### 推荐布局

- 左上：核心锚点区
  - 当前状态
  - 当前节点
  - 阻塞或延期提示
  - 核心金额
- 右上：高频操作区
  - 发货
  - 收款
  - 指派
  - 推进节点
- 左下：流转追踪区
  - 最新 3 条动态
  - 可展开完整时间线
- 右下：业务实体区
  - `Tab` 切换 BOM、物料清单、财务明细、备注等

补充原则：

- 不要把所有字段硬塞进首屏
- 低频信息通过抽屉、二级 Tab、弹窗展开
- 首屏只保留决策相关信息

优先改造页面：

- `web-admin/src/views/order/detail/index.vue`
- 工作台首页（如果后续补控制台页）

---

## 三、视觉规范工程化

不能只停留在“统一颜色和圆角”，需要进入代码层面的工程化约束。

### 1. 建立语义化 Token

不要在业务页面里直接写：

- `#ff4d4f`
- `red`
- 固定阴影值
- 各自定义的圆角值

应统一抽象为语义变量，例如：

- `--status-warning-bg`
- `--status-warning-text`
- `--status-success-bg`
- `--panel-radius`
- `--panel-shadow`
- `--panel-border`

### 2. 沉淀业务原子组件

以下组件应优先抽离：

- `BusinessMetricCard`
- `StatusBadge`
- `PageFilterBar`
- `EmptyStateGuide`
- `ActionPanel`

目标：

- 新页面通过拼装组件完成搭建
- 从物理层面减少视觉风格漂移
- 降低后续页面改版成本

### 3. 使用原则

- 页面只消费语义变量和业务组件
- 不在单页里重复定义状态色和统计卡样式
- 状态标签、卡片、筛选栏、空态引导风格统一

---

## 四、柔性过渡与未完成功能页

“空白页”不应只是简单提示或纯文本占位，而应升级为“业务流引导页”。

### 1. 前置依赖引导

适用场景：

- 物料页没有数据
- 客户页没有客户
- 某模块需要先初始化基础数据

推荐做法：

- 使用统一的 `EmptyStateGuide`
- 清楚说明当前为什么没有内容
- 提供直接动作按钮，例如：
  - “去新增首个物料”
  - “去录入客户”
  - “去配置分类”

### 2. 灰度 / 降级展示

适用场景：

- 接口未完成
- 功能还在开发中

推荐做法：

- 显示完整骨架屏或结构化占位
- 增加锁态遮罩或研发中提示
- 明确标注预计开放版本

目标：

- 比“白板页面”更专业
- 让用户知道功能方向和当前状态

---

## 五、建议实施顺序

建议按以下顺序推进：

1. 先做 `semantic tokens + 业务原子组件`
2. 以 `web-admin/src/views/request/list/index.vue` 作为 `Master-Detail` 样板页
3. 改造 `web-admin/src/views/order/detail/index.vue` 为 `Bento Dashboard`
4. 将客户、物料、库存、作品页逐步切到 `Directory` 模式

---

## 六、下一次继续时的直接任务

如果下次继续开发，优先从这里开始：

1. 在 `web-admin` 中建立页面级语义 Token
2. 抽离 `BusinessMetricCard`、`StatusBadge`、`EmptyStateGuide`
3. 先重构 `web-admin/src/views/request/list/index.vue`

这条路径最稳，也最容易在短时间内看到整体质感提升。
